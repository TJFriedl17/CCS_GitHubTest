

/**
 * main.c
 */
void main(void)
{
    /**
     * First, we are going to want to make sure that everything is enabled...
     * Should we represent this with a macro function?
     */
    // all_sensors_init();



    /**
     * Then, we should check to see if the servos are calibrated correctly. This should be commented out for final project
     */
    // servo_cal();


    /**
     * At this point, we want to see if we are going to be in manual driving mode or automatic. WE SHOULD BE ABLE TO SWITCH WHENEVER.
     * This being said, there needs to be a clear set of flags in the code to be able to switch whenever called from putty.
     */


}
